name: Windows Web GUI (Cloudflare)

on:
  workflow_dispatch:

jobs:
  windows-gui:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: تحميل وتثبيت VNC سريع ومباشر
      run: |
        $installer = "tightvnc.msi"
        Invoke-WebRequest -Uri "https://www.tightvnc.com/download/2.8.27/tightvnc-2.8.27-gpl-setup-64bit.msi" -OutFile $installer
        Start-Process msiexec.exe -ArgumentList "/i $installer /quiet /norestart SET_PASSWD=0" -Wait
        
        Start-Process -FilePath "C:\Program Files\TightVNC\tvnserver.exe" -ArgumentList "-run"
        Start-Sleep -Seconds 2

    - name: إعداد محول الويب noVNC
      run: |
        git clone https://github.com/novnc/noVNC.git
        pip install numpy websockify
        Start-Process -NoNewWindow -FilePath "python" -ArgumentList "-m", "websockify", "6080", "127.0.0.1:5900", "--web", ".\noVNC"

    - name: تشغيل نفق Cloudflare واستخراج الرابط
      run: |
        Invoke-WebRequest -Uri "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe" -OutFile "cloudflared.exe"
        Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --url http://localhost:6080" -RedirectStandardError "cf_log.txt" -NoNewWindow
        
        Start-Sleep -Seconds 12
        
        $url = Select-String -Path "cf_log.txt" -Pattern "https://[a-z0-9-]+\.trycloudflare\.com" | ForEach-Object { $_.Matches.Value }
        
        Write-Host "=========================================="
        Write-Host "✅ تم الاتصال بنجاح!"
        Write-Host "👉 افتح هذا الرابط في متصفح هاتفك:"
        Write-Host "$($url)/vnc.html"
        Write-Host "=========================================="

    - name: إبقاء الجلسة نشطة
      run: |
        while ($true) { Start-Sleep -Seconds 60 }
